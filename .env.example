# ============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES (NO VITE_ PREFIX!)
# ============================================================================
# These variables are ONLY accessible on the server (Vercel Functions)
# NEVER use VITE_ prefix for secrets - it exposes them to the browser!

# Database Configuration
# Get your database URL from Neon (neon.tech) or another PostgreSQL provider
# Format: postgresql://username:password@hostname/database?sslmode=require
DATABASE_URL=postgresql://username:password@hostname/database?sslmode=require

# Alternative name (Vercel sometimes uses this)
POSTGRES_URL=postgresql://username:password@hostname/database?sslmode=require

# JWT Secret for Authentication
# IMPORTANT: Generate a strong, random secret key for production
# You can use: openssl rand -base64 32
# NEVER commit your actual secret to version control!
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ============================================================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (WITH VITE_ PREFIX)
# ============================================================================
# These variables ARE exposed to the browser - only use for public config

# API Base URL (optional - defaults to same origin)
VITE_API_BASE_URL=

# Feature flags (example)
VITE_ENABLE_ANALYTICS=true

# Developer Godmode Panel - Allows quick navigation to any page during development
# Set to "true" to enable the floating developer panel with quick access to all routes
# This bypasses normal route protection to allow testing any page
# ⚠️ IMPORTANT: Set to "false" or remove this variable before production deployment!
# VITE_GODMODE=true

# ⚠️ WARNING: NEVER put database credentials with VITE_ prefix!
# ⚠️ NEVER: VITE_POSTGRES_URL - This would expose your database to anyone!
# ⚠️ NEVER: VITE_DATABASE_URL - This would expose your database to anyone!
# ⚠️ NEVER: VITE_JWT_SECRET - This would expose your secret key!
