// Import the Neon serverless driver\nimport { neon } from '@neondatabase/serverless';\nimport dotenv from 'dotenv';\n\n// Load environment variables from .env file\ndotenv.config();\n\n// In Node.js scripts, we use process.env\nconst databaseUrl = process.env.VITE_POSTGRES_URL;\n\nif (\!databaseUrl) {\n  throw new Error('Database URL not found in environment variables. Please check your .env file.');\n}\n\nconst sql = neon(databaseUrl);\n\nexport default sql;
